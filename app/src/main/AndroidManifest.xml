<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="google.login.turismo.appturismo">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TurismoLlanera"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menu" />
        <activity android:name=".activity_gastronomia" />
        <activity android:name=".activity_ocio" />
        <activity android:name=".activity_transporte" />
        <activity android:name=".activity_alojamiento" />
        <activity android:name=".activity_servicios" />
        <activity android:name=".activity_interes" />
        <activity android:name=".Detalles" />
    </application>

</manifest>